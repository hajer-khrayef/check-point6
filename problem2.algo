
let n = 3;
   
 
     PROCEDURE dotProduct(vect_A, vect_B)
    {
        let ps = 0;
        for (let i = 0; i < n; i++)
            ps = ps + vect_A[i] * vect_B[i];
              if(dotProduct(vect_A[i], vect_B[i]) === 0){
          console.log(`${vect_A[i]} and ${vect_B[i]} are orthogonal `)
              }
    }


    function dotProduct(vect_A, vect_B)
    {
        let ps = 0;
        for (let i = 0; i < n; i++)
            ps = ps + vect_A[i] * vect_B[i];
        return ps;
    }